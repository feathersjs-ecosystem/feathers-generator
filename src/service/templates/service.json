{
  "require": "feathers-{{answers.model.name}}",
  "options": [{
    "paginate": "config.paginate",
    "Model": {
      "require": "./test.model",
      "options": [{ "path": "{{answers.model.name}}", "db": "{{options.name}}" }]
    }
  }],
  "filter": {
    "all": { "require": "./test.filter", "options": [] }
  }
}
